import { styled } from "styled-components";

export const NumberContainer = styled.div`
  display:flex;
  flex-direction:column;
  alignItems:center;
  padding:0.5rem;
  min-width:3.5rem;
  font-size:2rem;
`