import {styled} from "styled-components"

export const CatchPhraseContainer = styled.div`
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:0.5rem;
  padding:5rem 0;
`

export const CatchPhraseText = styled.div`
  font-size:1.8rem;
`